# DairyOne — Part 1 changes

This zip contains every file changed/added for Part 1 (Routes, Multi-Stop
Journeys, GPS Tracking & Navigation), with the same folder paths as your
repo at `swaggie25/DairyOne`. Drop these into your working copy (they will
overwrite the matching files), or apply `PART1_CHANGES.patch` with:

    git apply PART1_CHANGES.patch

## Files
- `src/components/milk-entry-form.tsx` — live geofence lock UI
- `src/hooks/useAgentContext.ts` — resolves today's route via route_assignments
- `src/hooks/useOfflineQueue.ts` — surfaces sync errors
- `src/lib/geo.ts` — haversine + watchCoords helpers
- `src/lib/offline-queue.ts` — flushes through the verified RPC
- `src/routes/_authenticated/agent.tsx` — trip start carries assignment/vehicle
- `src/routes/_authenticated/field-setup.tsx` — GPS pin capture, geofence
  defaults, new Assignments tab
- `src/routes/_authenticated/route-marking.tsx` — NEW: Route Marking screen
- `src/routes/_authenticated/trip.tsx` — sequence-lock enforcement
- `src/integrations/supabase/types.ts` — regenerated Supabase types
- `src/routeTree.gen.ts` — auto-generated by TanStack Router (adds the new route)
- `supabase/migrations/*.sql` — the two migrations, already applied live to
  project `uuhhyzxagzswcjfhngom`. Included here so your local repo/migration
  history matches the database.

## Database
Both migrations are **already applied** to your live Supabase project. You
do not need to run them again — they're included only so `git log` /
`supabase migration list` reflect what's actually in the database. If you
ever rebuild a fresh local Postgres from these migration files, run them in
filename order.
